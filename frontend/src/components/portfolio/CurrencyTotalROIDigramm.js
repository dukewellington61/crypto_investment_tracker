import React from "react";

const CurrencyTotalROIDigramm = ({ currencyTotal }) => {
  // roi = (purchase value * 100) / total value
  // create array which has purchase value over time --> this should propably happen in the parent component (at least the fn call) because it is needed in component
  // which has balance over time as well
  return <div></div>;
};

export default CurrencyTotalROIDigramm;
